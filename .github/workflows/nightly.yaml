name: Nightly

# Nightly canary build. Full implementation in ZV-057.
on:
  schedule:
    - cron: "0 4 * * *"
  workflow_dispatch:

jobs:
  nightly:
    name: Nightly canary (ZV-057)
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Nightly build pipeline implemented in ZV-057"
