name: Release

# Release pipeline is implemented in ZV-057.
# This stub validates the workflow file is parseable by GitHub Actions.
on:
  push:
    tags:
      - "v*.*.*"

jobs:
  release:
    name: Release (ZV-057)
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Full release pipeline implemented in ZV-057"
